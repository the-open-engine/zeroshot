name: Publish Covibes Bridge

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Run npm publish in dry-run mode'
        required: true
        default: true
        type: boolean
      publish_bridge:
        description: 'Publish @covibes/zeroshot bridge package'
        required: true
        default: true
        type: boolean
      deprecate_legacy:
        description: 'Deprecate @covibes/zeroshot versions before the bridge'
        required: true
        default: false
        type: boolean

permissions:
  contents: read
  id-token: write

jobs:
  publish-bridge:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: legacy/covibes-zeroshot-bridge

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'

      - name: Verify bridge metadata
        if: inputs.publish_bridge == true
        run: |
          node -e "
          const pkg = require('./package.json');
          if (pkg.name !== '@covibes/zeroshot') throw new Error('wrong package name');
          if (pkg.version !== '5.4.1') throw new Error('wrong bridge version');
          if (pkg.bin.zeroshot !== 'cli/index.js') throw new Error('missing zeroshot bin');
          console.log(pkg.name + '@' + pkg.version);
          "

      - name: Pack bridge
        if: inputs.publish_bridge == true
        run: npm pack --dry-run

      - name: Dry-run publish bridge
        if: inputs.publish_bridge == true && inputs.dry_run == true
        run: npm publish --dry-run --access public

      - name: Publish bridge
        if: inputs.publish_bridge == true && inputs.dry_run == false
        run: npm publish --provenance --access public

      - name: Configure npm token for deprecation
        if: inputs.deprecate_legacy == true
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "${NPM_TOKEN:-}" ]; then
            echo "::error::Set repository secret NPM_TOKEN to an npm token that can deprecate @covibes/zeroshot."
            exit 1
          fi
          npm config set //registry.npmjs.org/:_authToken "$NPM_TOKEN"

      - name: Deprecate legacy versions
        if: inputs.deprecate_legacy == true
        run: |
          npm deprecate '@covibes/zeroshot@<=5.4.0' 'Zeroshot has moved to @the-open-engine/zeroshot. Run: npm install -g @the-open-engine/zeroshot'
