const {
  DEFAULT_REFLECTOR,
  EXPLANATION_MAX_LENGTH,
  REFLECTOR_REGISTRY,
  TEMPLATE_REFLECTOR,
  buildGuidanceText,
  formatValidationFeedback,
  isValidReflection,
  reflectorId,
  resolveReflector,
} = require('lyo-kernel');

module.exports = {
  DEFAULT_REFLECTOR,
  EXPLANATION_MAX_LENGTH,
  REFLECTOR_REGISTRY,
  TEMPLATE_REFLECTOR,
  buildGuidanceText,
  formatValidationFeedback,
  isValidReflection,
  reflectorId,
  resolveReflector,
};
